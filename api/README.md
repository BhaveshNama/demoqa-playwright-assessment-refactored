# API modules

Add API client modules here when a real endpoint is supplied. The current assessment provides only a JSON response contract, so tests validate local JSON datasets without making an unrelated external request.
